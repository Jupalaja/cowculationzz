package com.jupalaja.calorieCounter.infra.input.adapters.http

// Every http adapter should be created separately, notice that the http adapters don't require the use of a port
class ExampleHttpAdapter {
}
